# AGENTS.md — AIエージェント向け作業ルール

このリポジトリで作業する AIエージェントはこのファイルを最初に読むこと。

## リポジトリの性質

- 静的HTML（フレームワーク・ビルドツールなし）
- GitHub Pages でホスティング（`main` ブランチへのマージで自動デプロイ）
- スマホ（390px幅）を主対象とする教材サイト

## ファイル規約

| パス | 役割 |
|------|------|
| `index.html` | トップページ。教材カード一覧 + 総合ミニクイズ |
| `styles.css` | トップページ専用スタイル |
| `app.js` | トップページのクイズロジック |
| `lesson.css` | 教材ページ共通スタイル（lessons/*.html が読み込む） |
| `lesson.js` | 教材ページ共通クイズエンジン（lessons/*.html が読み込む） |
| `lessons/*.html` | 1論点1ファイルの教材ページ |
| `docs/content-guideline.md` | 教材コンテンツの書き方ルール（必読） |
| `.github/workflows/deploy.yml` | デプロイワークフロー（原則変更不要） |

## 教材ページの型

`lessons/` 以下の教材ページには以下の型がある。新規追加前に、どの型に該当するかを `docs/learning-architecture.md` で確認すること。

### 1. 標準説明教材型

従来の5部構成を使うページ。構成順序は以下のとおり。

1. **まず結論** — 1〜2文で論点の核心を述べる
2. **初心者向け説明** — 判例定義や条文を引用しつつ平易に解説
3. **具体例** — ○/×の対比形式
4. **試験で問われるポイント** — 箇条書き、判例名・年月日・判決種別を明記
5. **ミニクイズ3問** — 択一式、解答後に解説を表示

### 2. 基礎演習教材型

`docs/content-guideline.md` と `docs/operation-checklist.md` で定義された7セクション構成を使うページ。
一撃定義・まずイメージ・よくある勘違い・判例を場面で覚える（または場面で覚える）・1分復習・ミニクイズ・参考折りたたみ。

既存の `lessons/shobunsei.html`・`lessons/genkoku-tekikaku.html`・`lessons/uttae-no-rieki.html`・`lessons/shusso-kikan.html` がこの型に該当する。

### 3. 横断判定ドリル型

`docs/learning-architecture.md` で定義された第3層（横断判定ドリル）の教材。
複数論点のうち、どこをまず疑うべきかを判断するページ。セクション構成は教材設計に従って柔軟に決める。

`lessons/torikeshi-entry-drill.html` がこの型に該当する。

### 型の共通ルール

- 法的正確性・クイズ正答・確認状態・source-log管理は型によらず厳守する
- 新しい教材型を作る場合は、先に `docs/` に設計を記録してから作る
- `docs/content-guideline.md`・`docs/operation-checklist.md`・`docs/learning-architecture.md` の方針に従うこと

## コーディング規約

- `lessons/*.html` は `<link rel="stylesheet" href="../lesson.css">` で読み込む
- `lessons/*.html` のクイズデータはページ内 `<script>` で `const answers = {...}` として定義し、その後 `<script src="../lesson.js"></script>` を読み込む
- 外部ライブラリ（CDN含む）は使用しない
- `max-width: 480px` + `padding: 0 16px` でセンタリング
- フォントは `-apple-system, BlinkMacSystemFont, "Hiragino Sans", sans-serif`
- 横スクロールが発生しないこと（390px で必ず確認）
- `<a>` のリンク先は相対パスで記述する（例: `../index.html`）

## 新規教材追加の手順

1. `TASK_TEMPLATE.md` をコピーして Issue を作成
2. `docs/learning-architecture.md` で教材型（標準説明教材型・基礎演習教材型・横断判定ドリル型）を確認する
3. 該当する型のガイドライン（`docs/content-guideline.md` 等）を読み、ルールに従って `lessons/<slug>.html` を作成
4. `index.html` への導線追加はページ内容確認後に別PRで行う（任意）
5. `README.md` のファイル構成表を更新
6. コミット → プッシュ → PR → main にマージ

## 確認フロー（条文・判例の確認状態）

`docs/source-log.md` の確認状態は以下の4段階で管理する。

| 状態 | 定義 |
|------|------|
| `AI記載` | AIが作成したが、他AIまたはレビュアーによる照合が未完了 |
| `AIクロスチェック済み` | Claude Code以外のAIレビュー（Codex Review・ChatGPTレビュー等）を経て、明確な指摘への対応が完了している状態 |
| `人間最終確認待ち` | AIクロスチェック済みだが、人間による最終承認前の状態 |
| `人間最終確認済み` | ユーザーが確認対象・確認URL・確認日・確認者を明示し、最終確認済みであると明確に指示した状態 |

状態遷移は `AI記載` → `AIクロスチェック済み` → `人間最終確認待ち` → `人間最終確認済み` の順。ステップのスキップは原則不可。

> 注: 旧表記 `人間確認済み` は移行期の既存エントリのみ存続する。新規エントリには `人間最終確認済み` を使用すること。

## 並列教材作成フロー（設計方針）

複数の教材を同時並行で進めるための設計方針を定める。**この節は設計方針の明文化であり、`docs/source-log.md` や `README.md` などの共有ファイルを本文PRから外す運用は、PR-B（CI整備PR）で `.github/workflows/pr-scope-check.yml` の許可ファイルを更新してから正式適用する。** PR-B が main にマージされるまでは現行の Scope Check ルール（教材本文追加PR = `lessons/*.html` + `README.md` + `docs/source-log.md`）に従うこと。

### 役割分担（PR-B merge後に正式適用）

- **本文PR（教材本文追加PR）** … `lessons/*.html` **のみ**を変更する。法的主張・クイズ・本文の作成とCodexループレビューに専念する。
- **後処理PR（教材後処理PR）** … 共有ファイル（`docs/source-log.md`・`README.md` のファイル構成表・`index.html` の導線・`docs/card-roadmap.md`）への変更を**ここに集約**する。後処理PRは直列（1本ずつ）で進める。
- ねらいは、並列で進む本文PRが互いに別ファイル（別の `lessons/*.html`）だけを触る状態にし、共有ファイルでの技術的競合を構造的に避けること。

> 移行期間中（PR-B merge前）は、現行どおり本文PRが `README.md`・`docs/source-log.md` を含んでよい。新フローの「本文PR = `lessons/*.html` のみ」「`README.md` を後処理PRへ移す」は、PR-B で Scope Check を更新した後に完全適用する。

### 並列の上限と単位

- 並列上限は**当面2本まで**。
- 並列する本文PRは**必ず別クラスタ**（行政法総論／行政手続法／行政不服審査法／行政事件訴訟法／国家賠償・損失補償／地方自治／民法など）のカードにする。
- **同一クラスタのカードを並列で進めることは禁止**。同一クラスタは制度境界の枠組み（例「処分または裁決の取消し」）を共有しており、先行カードの修正を後続カードへ反映できなくなるため。
- source-log の個別ファイル化（`docs/source-log/lessons/<slug>.md`）と、それに伴う3本並列化は、**将来オプションとして記録するにとどめる**（本フローでは実施しない）。

### main追従とレビュー順序

- 先行PRが main にマージされたら、後続PRは **`git merge origin/main` で main を取り込んでから** Ready化または最終レビューに進める。
- 追従せずに Ready化・最終レビュー・merge へ進めない。

### P2伝播チェック（必須）

- 先行PRで Codex の P1/P2 指摘、または ChatGPT 最終レビューによる修正が出た場合、後続PRでは**同じ制度境界が後続カードにも当てはまらないかを必ず点検する**（P2伝播チェック）。
- 特に横断的な境界語（例「処分**または裁決**の取消し」「**法令に基づく申請に対する**不作為」）は、兄弟カード・関連カードの本文と source-log の双方で表現が揃っているかを確認する。

### 後処理PRでの source-log 記録の扱い（例外の明文化）

- 後処理PRで**新規作成する** source-log エントリは、本文PRでの Codexループレビュー・Ready後Codexレビュー・ChatGPT最終レビュー・merge後の人間表示確認OKをすべて根拠として、確認状態を**最初から `AIクロスチェック済み` で記録してよい**。本文PRと後処理PRを分離した結果、ファイル履歴上は `AI記載` ステップを経由しないことを許容する。
- ただし、この例外を適用しても以下の保護ルールは必ず維持する。
  - 確認URLは `要確認` のままにする（一次資料URLを推測で記載しない）。
  - `人間最終確認待ち`・`人間最終確認済み`・一次資料確認済みには**しない**。
  - `人間最終確認済み` への昇格は、従来どおりユーザーが4点（確認対象・確認URL・確認日・確認者）をすべて明示し、最終確認済みであると明確に指示した場合に限る。

## やってはいけないこと

- `main` ブランチに直接プッシュしない
- ビルドツール（webpack, vite 等）を導入しない
- 教材の法令・判例内容を根拠なく変更しない（一次資料で確認すること）
- `lesson.css` / `styles.css` にページ固有のスタイルを追加しない
- 判例表記から年月日・判決種別を省略しない（content-guideline.md §2 参照）
- `docs/source-log.md` の「確認状態」を AIエージェントの独自判断で `人間最終確認済み` に変更しない。AIが新規追加するエントリは必ず `AI記載` から始めること
- AIクロスチェック（Codex Review・ChatGPTレビュー等）を経て指摘への対応が完了した場合は `AIクロスチェック済み` まで更新してよい。人間の最終確認対象として明示的にキューへ移す場合は `人間最終確認待ち` に更新してよい
- **例外（人間最終確認結果の転記）**: ユーザーが以下の4点をすべて明示し、かつ「最終確認済みであること」を明確に指示した場合に限り、AIエージェントはその確認結果の転記として確認状態を `人間最終確認済み` に更新してよい
  1. 確認対象（条文名・判例名）
  2. 確認URL（一次資料の直接リンク）
  3. 確認日
  4. 確認者（人名またはアカウント）
- この例外を適用する場合、PR本文に「確認対象・確認URL・確認日・確認者・未確認のまま残す項目」を必ず明記すること
- ユーザーが確認を明示していない条文・判例を、まとめて `人間最終確認済み` にしてはいけない
- AIクロスチェックを実施した場合、PR本文に「チェック対象・使用した資料URL・レビュー担当・採用/却下の判断・未確認項目」を必ず明記すること
